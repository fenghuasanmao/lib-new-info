<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-iYQeCzEYFbKjA/T2uDLTpkwGzCiq6soy8tYaI1GyVh/UjpbCx/TYkiZhlZB6+fzT" crossorigin="anonymous">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>练习</title>
    <style>
        h1{
            text-align: center;
            margin-top: 20px;
        }
        .card-zidingyi{
            display: flex;
            justify-content: space-between;
            align-items: center;
            font-size: 14px;
        }
        .card-zidingyi p{
            margin-right: 50px;
        }
    </style>
</head>
<body>
    <!-- <% for(let i=0; i< result.length;i++){ %>
       <h1> <%= result[i].title %></h1>
       <% for(let j=0;j< result[i].news_list.length;j++){ %>
        <%= result[i].news_list[j].news_time %>
        <%= result[i].news_list[j].news_title %>
        <br>
        <a href="<%= result[i].news_list[j].news_link %>"><%= result[i].news_list[j].news_link %></a>
        <br>
       <% } %>
    <% } %> -->
<h1>热点高校图书馆最新通知速览</h1>
    <div class="row mt-3">
        <% for(let i=0; i< result.length;i++){ %>
        <div class="column col-md-3">            
            <div class="card mb-4">
                <div class="card-header">
                    <%= result[i].title %>
                </div>
                <% for(let j=0;j< result[i].news_list.length;j++){ %>
                <div class="card-body card-zidingyi">
                    <div class="card-neirong">
                  <p class="card-title"><%= result[i].news_list[j].news_title %></p>
                  <p class="card-text"><%= result[i].news_list[j].news_time %></p>
                </div>
                  <a href="<%= result[i].news_list[j].news_link %>" class="btn btn-sm btn-primary">查看</a>
                </div>
                <% } %>
              </div>         
        </div>
        <% } %>
    </div>

</html>